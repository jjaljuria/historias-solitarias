import { describe, expect ,it} from "vitest";
import UserController from './UserController';

describe('UserController', ()=>{
	it('UserController.ts has been defined', ()=>{
		expect(UserController).toBeDefined()
	})

	it('should UserController has ')
})